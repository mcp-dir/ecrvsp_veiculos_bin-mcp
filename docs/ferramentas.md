# Ferramentas

ECRVSP Veículos: Cadastro BIN RENAVAM expõe 1 ferramenta (todas somente leitura).

### 1. `ecrvsp_veiculos_bin_consultar`
**Input**: `a3`, `a3_pin`, `login_cpf`, `login_senha`, `chassi` (opcional), `placa` (opcional), `renavam` (opcional), `credenciais_lista` (opcional)

ECRVSP Veículos: Cadastro BIN RENAVAM, consulta em fonte oficial.

## Prompts de exemplo

```
Consultar ECRVSP Veículos: Cadastro BIN RENAVAM
```
